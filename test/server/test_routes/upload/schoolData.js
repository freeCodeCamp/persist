const expect = require('expect');
const request = require('supertest');

describe('/upload/schoolData', () => {
  describe('POST', () => {
    it.skip('should save valid schoolData uploaded as a csv file', () => {});

    it.skip('should return a status 500 error if the schoolData is invalid', () => {});

    it.skip('should return a status 500 error if no data is uploaded', () => {});
  });
});
