const expect = require('expect');
const request = require('supertest');

describe('/upload/collegeData', () => {
  describe('POST', () => {
    it.skip('should save valid collegeData uploaded as a csv file', () => {});

    it.skip('should return a status 500 error if the collegeData is invalid', () => {});

    it.skip('should return a status 500 error if no data is uploaded', () => {});
  });
});
