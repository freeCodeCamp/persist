const expect = require('expect');
const request = require('supertest');

describe('/upload/applicationData', () => {
  describe('POST', () => {
    it.skip('should save valid applicationData uploaded as a csv file', () => {});

    it.skip('should return a status 500 error if the applicationData is invalid', () => {});

    it.skip('should return a status 500 error if no data is uploaded', () => {});
  });
});
